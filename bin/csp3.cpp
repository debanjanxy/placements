#include <iostream>
#include <algorithm>
#include <cmath>
#include <cstring>
#include <iterator>
#include <map>
#include <vector>


using namespace std;

int main(){
	long long n,k;
	cin >> n >> k;
	vector <long long> f,h;
	for(vector<long long> iterator i=f.begin(); i!=f.end(); i++){
		int x;
		cin >> x;
		f.push_back(x);
	}
	for(vector<long long> iterator i=h.begin(); i!=h.end(); i++){
		int y;
		cin >> y;
		h.push_back(y);
	}
	
